# QA Automation Challenge

This repository contains a basic scaffold for a QA automation project.

## Structure
- `tests/` for automated test cases
- `pages/` for page object models
- `test-data/` for JSON test data
- `reports/` for generated reports
- `test-plan/` for the test strategy document
